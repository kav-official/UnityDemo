using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class BackSpace : MonoBehaviour
{
    void Start()
    {
        // GameObject gameobject = GameObject.Find("TextBackSpace");
        // gameobject.GetComponent<TextMeshProUGUI>().text = "⌫";
    }
}
